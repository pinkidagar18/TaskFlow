{
  "name": "taskflow-team-manager",
  "version": "1.0.0",
  "description": "Full-stack Team Task Manager Assignment",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "cd backend && nodemon server.js",
    "install-deps": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
